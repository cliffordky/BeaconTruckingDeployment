pipeline {
  agent {label 'agent1'}

  stages {
    stage('Install IIS') {
      steps {
        sh '''
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
          ansible -m win_ping windows_servers -i inventory/hosts
        '''
      }
    }
  }
}